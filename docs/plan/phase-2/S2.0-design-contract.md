# Step 2.0 — The Phase 2 prototype plates

**Branch:** `feat/phase-2-workout-log` · **Size:** M · **Depends on:** Phase 1
**Unlocks:** 2.3a, 2.9b · **Skill to load:** `design-system`

The prototype is the visual contract. Today it has no plate for 5 of the Phase 2
screens. A UI step cannot be checked against a plate that does not exist.

## Context to read first

1. `.claude/skills/design-system/` — load it first.
2. The mockup: https://claude.ai/artifact/8pajWGN8ykJNhxaGLSNNG4.
3. `docs/design/prototype/index.html` — plates `p3` Live workout and `p4` Exercise
   picker already exist. They show the style.
4. `.claude/skills/design-system/references/charts.md`.

## State before the step

Phase 1 is merged. The prototype has plates for the live workout and the picker only.
It has no rest timer state, no finish offer, no "Manage exercises", no import review,
and no Phase 2 charts.

## Goal

The repository holds the visual contract for every Phase 2 screen before any Phase 2
UI code exists.

## Do

1. Add 5 plates to `docs/design/prototype/index.html`, numbered after the last plate:
   - Rest timer · `/workout`, the cyan card, with Skip and +30 s
   - Finish and the gym time offer · `/workout` after Finish, and the same offer on
     `/log`
   - Manage exercises · `/profile`, rename, archive, restore
   - Import review · `/profile`, the row table, the 2 readings side by side, "Apply to
     all similar", and Skip, Overwrite or Merge for an existing date
   - Analytics, Phase 2 · volume load, one-rep max, the record list, muscle balance
     with its warning, the gap histogram, calories a minute
2. Reuse the prototype classes. Add a new class only when no class fits, and name it
   in the plate's comment block.
3. Draw the 1440 px layout of `/workout` in the laptop section: the exercise list on
   the left and the active exercise on the right.

## Acceptance criteria

- [ ] The 5 plates match the mockup: the layout, the copy and the order of parts.
- [ ] Every color in the new plates comes from a `--color-*` token.
- [ ] Every button in the new plates has an explicit `type`.
- [ ] The muscle balance warning and every `review` cell carry text, not color only.
- [ ] `pnpm design:check` passes.
- [ ] The owner opened the prototype and accepted the plates.

## Tests

None in code. The plates are the contract that the UI steps test against.

## Verification

1. Open `docs/design/prototype/index.html` in a browser at 390 px and at 1440 px.
2. `pnpm verify`.

## Files touched

- `docs/design/prototype/index.html`

## Risk

The mockup can disagree with the plan text. The plan text wins for behaviour. The
mockup wins for the look. Record each disagreement in `PROGRESS.md` under "Settled".

## When the step is done

1. `pnpm verify`
2. The `commit-report` skill, with screenshots of each new plate.
3. Commit `docs(design): add the phase 2 prototype plates`
4. Set **Next step** in `PROGRESS.md` to `2.1a`.

## Checkpoint 0

Stop here. The owner accepts the plates before any UI step. See `index.md`.
