# Step 2.11 — Phase 2 verification and the device check

**Branch:** `feat/phase-2-workout-log` · **Size:** S · **Depends on:** all
**Unlocks:** Phase 3 · **Agent to use:** `ui-reviewer`

## Context to read first

1. `docs/plan/02-phase-2.md`, section 2.11 — the checklist.
2. `tests/e2e/responsive.spec.ts` and `tests/e2e/a11y.spec.ts`.
3. `docs/plan/phase-1/S1.17-deploy-device-check.md` — how the device check ran.

## State before the step

Every Phase 2 step is done. The new routes are not yet in the responsive and axe
suites.

## Goal

Every item of the Phase 2 checklist passes, on the laptop and on the iPhone.

## Do

1. Add `/workout`, the picker sheet, the import review and the new `/analytics`
   charts to `tests/e2e/responsive.spec.ts` and `tests/e2e/a11y.spec.ts`.
2. Check that `/styleguide` still matches the prototype.
3. Deploy the branch preview on Vercel.
4. The owner runs the device check on the iPhone:
   - A full session of 3 exercises with the phone in airplane mode.
   - The rest timer after the screen locks.
   - The resume prompt after the app is closed.
   - The export download.
   - Online again: every set appears in Supabase.
5. Tick each checklist item in `docs/plan/02-phase-2.md` and in `index.md`.

## Acceptance criteria

- [ ] `pnpm verify` passes.
- [ ] `pnpm e2e` passes on all 3 projects.
- [ ] `lib/metrics/**` coverage is 100 percent for lines.
- [ ] `lib/csv/**` coverage is 95 percent or higher.
- [ ] Zero serious axe issues on `/workout` and the picker sheet.
- [ ] No horizontal scroll from 320 px to 2560 px on the new screens.
- [ ] The owner passed the device check.

## Verification

1. `pnpm verify`.
2. The device check above.

## Files touched

- `tests/e2e/responsive.spec.ts`
- `tests/e2e/a11y.spec.ts`
- `docs/plan/02-phase-2.md`
- `docs/plan/phase-2/index.md`
- `PROGRESS.md`

## When the step is done

1. `pnpm verify`
2. The `commit-report` skill.
3. Commit `test(test): cover the phase 2 routes and close the phase`
4. Mark Phase 2 done in `PROGRESS.md`. Set **Next step** to `3.1`.

## Checkpoint D

Stop here. Report to the user. Phase 3 starts only after the owner approves.
