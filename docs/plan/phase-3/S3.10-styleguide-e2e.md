# Step 3.10 — Style guide, access and the end-to-end suite

**Branch:** `feat/phase-3-coach-plans` · **Size:** M · **Depends on:** 3.9
**Unlocks:** 3.11 · **Skills to load:** `design-system`

## Context to read first

1. `app/styleguide/page.tsx` — the live design contract.
2. `tests/e2e/responsive.spec.ts`, `tests/e2e/a11y.spec.ts` and the signed-in fixture
   from step 1.16.
3. `.claude/rules/accessibility.md`.

## State before the step

The Phase 3 screens work. `/styleguide`, the responsive suite and the axe suite do
not know them.

## Goal

The machine proves that the Phase 3 screens match the contract, work offline, and pass
access checks.

## Do

1. `/styleguide` gains 4 samples: `PlanCard` selected and not selected,
   `StepImageCard`, `StepProgress` with every status, and the Start bar.
2. Add `/workouts`, `/workouts/chest-triceps` and `/workout` in plan mode to the
   responsive suite and the axe suite.
3. `tests/e2e/plans.spec.ts`, on all 3 projects:
   1. Open `/workouts`. The hero names the suggested plan.
   2. Select Shoulders and Legs. Start. The header reads `Shoulders + Legs`.
   3. Go offline. Log a set on step 1. Skip step 2. Log a set on step 6.
   4. Reload. The session resumes at step 6, and step 2 still shows as skipped.
   5. Every image on the screen has loaded, with the network still down.
   6. Finish. The summary reads `2 of 8 steps`.
   7. Go online. Every set reaches Supabase with the session's `plan_ids`.

## Acceptance criteria

- [ ] `/styleguide` matches the prototype plates.
- [ ] axe reports zero serious or critical issues on the 3 routes.
- [ ] The 3 routes have no horizontal scroll from 320 px to 2560 px.
- [ ] Every tap target on the 3 routes is 44 px or taller.
- [ ] `tests/e2e/plans.spec.ts` passes on iPhone 14, Pixel 7 and desktop 1440.

## Verification

1. `pnpm e2e`.
2. `pnpm verify`.

## Files touched

- `app/styleguide/page.tsx` and its test
- `tests/e2e/responsive.spec.ts`
- `tests/e2e/a11y.spec.ts`
- `tests/e2e/plans.spec.ts`

## When the step is done

1. `pnpm verify`
2. The `commit-report` skill, with screenshots of `/styleguide` at 3 widths.
3. Commit `test(test): add the phase 3 style guide samples and end-to-end suite`
4. Set **Next step** in `PROGRESS.md` to `3.11`.
