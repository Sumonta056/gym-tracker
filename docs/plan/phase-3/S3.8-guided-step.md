# Step 3.8 — `/workout` in plan mode, the guided step

**Branch:** `feat/phase-3-coach-plans` · **Size:** L · **Depends on:** 3.7
**Unlocks:** 3.9 · **Skills to load:** `design-system` · **Agent to use:** `ui-reviewer`

## Context to read first

1. `.claude/skills/design-system/` — load it first.
2. The prototype plate "Guided step" from step 3.1.
3. `app/(app)/workout/` — the Phase 2 live session: the timer, the set rows, the rest
   timer and the resume prompt.
4. `components/ui/AppShell.tsx` and `tests/e2e/responsive.spec.ts`.
5. `lib/metrics/planProgress.ts` and the repository from 3.5 and 3.6.

## State before the step

`/workout` runs a free Phase 2 session. It ignores `plan_ids`.

## Goal

The user goes through a plan one exercise at a time, sees how to do each one, and logs
every set, with the least taps.

## Do

1. `/workout` reads the active session. With `plan_ids`, it shows plan mode. With
   null, it shows the Phase 2 free session, unchanged.
2. `PlanStepHeader`:
   - Left, 44 × 44: a back button to the previous step. On step 1, a leave link to
     `/workouts` instead. The session stays active when the user leaves.
   - Centre: the plan title, then `Step 2 of 9 · Chest`.
   - Right: `Elapsed` and the time from `started_at`.
3. `StepProgress`: an `<ol>` with 1 bar per step. The color comes from
   `stepStatuses`. Each `<li>` holds a visually hidden label, for example
   `Step 2, Flat Dumbbell Press: now`.
4. `StepImageCard`:
   - The start and end images from `step.images`, side by side, 3 : 2, with `next/image` and a fixed
     aspect ratio, so the layout does not jump.
   - The labels `1 · Start` on `--color-ground` and `2 · End` on `--color-accent`.
   - The alt texts `<name>, start position` and `<name>, end position`.
   - The name as the page `<h1>`, the muscle group, the how-to text.
   - The pills `Target 3 × 10–12` and `Last time 14 kg × 10`.
   - A failed image keeps its box and shows the name on `--color-surface-2`.
5. The set rows are the Phase 2 rows. On the first visit to a step, create 3 rows from
   `lastSetFor`. A first time leaves the weight empty with the hint `Pick a light
weight`. A body-weight step shows `Body` and stores a null weight.
6. A tick starts the Phase 2 rest timer. The rest card sits under the sets.
7. The foot, in a fixed bar: `Skip` and `Next step`, with the next name under it in
   small text. The last step shows `Finish session`. Skip adds the step to
   `cursor.skipped`, then moves on.
8. Every move writes the cursor through `setPlanCursor` at once. A reload reads it and
   resumes. No cursor: resume at the first step with no sets.
9. `AppShell` gains `mode="focus"`. Under 1024 px it hides the bottom bar. At 1024 px
   and up the sidebar stays. Only `AppShell` writes the frame, so the design check
   still passes.
10. `tests/e2e/responsive.spec.ts`: add a named exception for `/workout` in plan mode
    under 1024 px, with the reason "focus mode, agreed on 2026-09-24, the header
    carries the leave link". Assert the leave link or the back button is visible
    instead.

## Acceptance criteria

- [ ] A free Phase 2 session still shows the Phase 2 screen.
- [ ] Next moves the cursor by 1 and saves it.
- [ ] Back moves the cursor back by 1. Step 1 shows the leave link.
- [ ] Skip records the step in `skipped` and moves on.
- [ ] A skipped step that later gets a set shows as done.
- [ ] The rows fill from `lastSetFor`, and are empty on a first time.
- [ ] `+ Add set` copies the row above.
- [ ] A tick starts the rest timer.
- [ ] A reload resumes at the same step, with the same skips.
- [ ] The elapsed time is correct after a reload.
- [ ] Every control is 44 px or taller. `Skip` and `Next step` are 54 px.
- [ ] No horizontal scroll at 320 px, even with `Dumbbell Overhead Extension`.

## Tests

- Component: `PlanStepHeader` on step 1 and on step 2.
- Component: `StepProgress` labels and statuses.
- Component: `StepImageCard` alt texts and the failed-image state.
- Component: Next, Back and Skip change the cursor.
- Component: the rows on a first time and on a repeat.
- Integration: a remount resumes at the same step, with the same skips.

## Verification

1. The `ui-reviewer` agent reports no drift against the plate.
2. Check `/workout` in plan mode at 320 px, 390 px, 768 px and 1440 px.
3. `pnpm verify`.

## Files touched

- `app/(app)/workout/page.tsx` and its test
- `components/plans/PlanSession.tsx` and its test
- `components/plans/PlanStepHeader.tsx` and its test
- `components/plans/StepProgress.tsx` and its test
- `components/plans/StepImageCard.tsx` and its test
- `components/plans/StepFooter.tsx` and its test
- `components/ui/AppShell.tsx` and its test
- `tests/e2e/responsive.spec.ts`

## Risk

This is the largest step. If it passes 1 session, split it: 3.8a for the header,
progress and image card, 3.8b for the rows, the cursor and focus mode. Record the split
in `index.md`.

## When the step is done

1. `pnpm verify`
2. The `commit-report` skill, with screenshots at 3 widths next to the plate.
3. Commit `feat(ui): add the guided plan step`
4. Set **Next step** in `PROGRESS.md` to `3.9`.
5. Stop at **Checkpoint B** in `index.md`.
