# Step 1.16 — The end-to-end suite

**Branch:** `feat/phase-1-daily-tracker` · **Size:** M · **Depends on:** 1.15
**Unlocks:** 1.17

## Context to read first

1. `.claude/rules/testing.md` — the end-to-end section.
2. `playwright.config.ts` — the three projects are iPhone 14, Pixel 7 and desktop 1440.
3. `lighthouserc.json` — the installability check.

## State before the step

Each step has added its own end-to-end test. No test proves the full offline journey
from start to end.

## Goal

One suite proves the Phase 1 exit condition on all three Playwright projects.

## Do

Write these eight tests. Run each one on all three projects.

1. An anonymous visit redirects to `/sign-in`.
2. Sign in, then land on the dashboard.
3. Record a day. The dashboard number changes.
4. Go offline. Record a second day. It appears at once and the chip reads `OFFLINE`.
5. Go online. The chip turns green and the row reaches Supabase.
6. Kill the app while offline, reopen it, and the unsynced write is still pending.
7. Analytics renders every chart with no console error.
8. Lighthouse passes installability.

## Acceptance criteria

- [ ] All eight tests pass on iPhone 14, Pixel 7 and desktop 1440.
- [ ] Test 5 proves the row is in Supabase, not only that the chip changed colour.
- [ ] Test 6 uses a real context reload, not only a page refresh.
- [ ] Test 7 fails on any console error, not only on a thrown error.
- [ ] No test is skipped on any project.
- [ ] No test depends on a row left behind by another test. Each one sets up and
      tears down its own data.

## Verification

1. `pnpm e2e`. All three projects green.
2. `pnpm lighthouse`. Installability passes.
3. `pnpm verify`.

## Files touched

- `tests/e2e/journey.spec.ts`
- `tests/e2e/offline.spec.ts`
- `playwright.config.ts`, if a new fixture is needed

## Risk

Test 5 needs a real Supabase test account. Use a separate account from the personal
one. Never put its key in a committed file.

## When the step is done

1. `pnpm verify`
2. The `commit-report` skill.
3. Commit `test(test): add the phase 1 end-to-end journey suite`
4. Set **Next step** in `PROGRESS.md` to `1.17`.
